// Which snippet could you add to this code to print "{"type":"tiger"}" to the console

let cat = { type: "tiger", size: "large" };

let json = /* Snippet here */ JSON.stringify(cat);

console.log(json);// prints {"type":"tiger"}